{% extends 'base.html.twig' %}

{% block title %}Hello HistoryglobalController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
<a href='/'>back home</a>
    <table>
        <tbody>
        {% for hist in historyg %}
            <tr>
                {# <th>Id</th> #}
                {# <td>{{ user_id }}</td> #}
                <th>Score</th>
                <td>{{ hist.result }}</td>
                <th>Answer</th>
                <td>{{ hist.answer|join(', ') }}</td>
            </tr>  
        {% endfor %}
        </tbody>
    </table>
</div>
{% endblock %}
